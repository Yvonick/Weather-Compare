# Optional credentials for national services that require registration.
FROST_CLIENT_ID=
# Preferred: the application credential from portail-api.meteofrance.fr.
METEOFRANCE_APPLICATION_ID=
# Alternative: a currently valid Bearer/API token (typically short-lived).
METEOFRANCE_API_KEY=
KNMI_API_KEY=
